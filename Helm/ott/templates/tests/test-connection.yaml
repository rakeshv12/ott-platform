apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ott.fullname" . }}-test-connection"
  labels:
    {{- include "ott.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ott.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
